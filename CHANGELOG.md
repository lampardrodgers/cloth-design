# Changelog

## V0.9.0 - 2026-08-20

- 画布：「放到画布」不再把浮层卸载重挂。以前 `InFrontOfTheCanvas` 写在 `useMemo` 里、依赖 `pendingImages`，每加一张待放图就造一个新组件，tldraw 按组件身份整层重挂，`busyFrameIds` / `consumedRef` 归零——面板按钮重新可点而画框还写着「正在生成…」，能重复提交。现在浮层组件固定，`pendingImages` 走 context。
- 画布：刷新 / 重挂之后遗留在 IndexedDB 里的「正在生成…」画框，onMount 时统一收口成失败并说明「刷新时断开了跟踪；如果这次出图成功，成片可以在成片库里找到」。简易 / 画布切换不再卸载画布（打开过就 `display:none` 保活），切布局时在跑的请求不会对着已销毁的 editor 写；看门狗量不到隐藏画布的尺寸时不再误判白屏。放进画布的受管成片先转成 data URL 存进 tldraw 资产，服务器 3 天清理后画布上不裂图。
- 简易模式的附件移出 localStorage（data URL 几张 PNG 就撑爆 5MB，撑爆时会悄悄砍掉后加的那几张），改存 IndexedDB（`src/lib/idbStore.ts`，按账号隔离、退出时一并清理）；写失败明确提示，不再静默。过期（服务器已清理）的成片在简易模式里显示「服务器副本已清理」而不是裂图，下载按钮收起。
- 后台「积分规则」「系统提示词模板」从管理员本机 localStorage 搬到服务端 `app_config`（`server/app-settings.mjs`）：`/api/me` 下发给每个账号，服务端 `estimateCredits` 扣费用的就是同一份，管理员改倍率对所有人生效；提示词模板按模式覆盖、可一键恢复内置默认。两个版块都改成「改草稿 → 保存」，页面上写清楚是对所有账号生效。新增 `PUT /api/admin/credit-policy`、`PUT /api/admin/system-prompts`。
- 删除成片改成软删除：点删除先从界面拿掉并给 5 秒「撤销」（底部提示条），到点才真的调接口删记录和服务器文件；页面关掉前还没到点的用 keepalive 请求补发。短视频删除加确认；排队中 / 生成中的短视频任务可以「取消」（新增 `POST /api/shortvideo/tasks/:id/cancel`：本站标 cancelled、不再轮询、引擎侧任务顺手删）。
- 会话失效统一处理：`parseJson` 遇到 401、或 403 里的「账号已锁定 / 待开通」，广播 `clothdesign:unauthorized`，App 收到后回登录页并说明「登录已失效」；本地命名空间里没提交的输入不清，重新登录还在。不再每个动作各自弹「请求失败: 401」。
- 视图进地址栏：`/free` `/studio` `/workflows` `/account` `/storage` `/shortvideo` 各占一个路径，刷新停在原处、浏览器前进后退有效、链接能分享；没权限的账号敲 `/shortvideo` 当作自由创作。功能中心第一次进去之后保活（hidden 不卸载），切视图回来表单、上传素材、轮询都还在。
- 批量与取消：「全部存到本地」有 N/M 进度条和「停止」；任务面板里运行中的出图任务可以「放弃等待」（中断这次请求；服务端照样出图，成片之后会同步进列表），简易模式收到放弃不再把描述放回输入框。
- 账户页新增「修改密码」（走 better-auth `change-password`，改完其它设备的登录态一并失效）。后台改密 / 配 Key 不再用明文的 `window.prompt`，改成行内 `type=password` 小表单；建号表单的初始密码也改成密码框（可点「显示」）。
- 提示词库、色卡、设置、模式草稿、自由创作参数、侧栏折叠、短视频表单等偏好跨设备同步：`user_profile.preferences_json` + `PUT /api/me/preferences`，登录时服务端那份先落到本地命名空间再渲染，本机改动防抖 1.5 秒推回；本机有、服务端没有的键首次登录时反向补推。任务 / 成片 / 附件不同步。
- 新增 `tests/server-app-settings.mjs`、`tests/client-review-fixes.mjs`（真浏览器：地址栏视图、软删除撤销、会话失效回登录、功能中心保活），`tests/server-shortvideo.mjs` 补取消用例。
- 第二轮 review 补漏（上面几条「部分修复」的收尾）：
  - 后台系统提示词真的进出图链路：创作台出图以前拿的是静态目录里的模式对象，后台改的模板到不了 `buildOptimizedPrompt`；现在 `handleGenerate` 把服务端模板套到模式上，自由创作 / 按标注改图 / 按草图生成也带上后台给 free 配的「模式提示」（默认为空，不配则一个字不变）。积分规则 / 模板不再只在登录时拉一次：新增 `GET /api/app-settings`，页面回到前台（切标签 / 聚焦）最多 30 秒拉一次、后台常驻每 5 分钟拉一次，管理员改完报价对开着的页面也生效。
  - 短视频取消不再被轮询改回去：轮询是「读行 → 等引擎 → 写回」，等引擎那几秒里用户取消了，旧行会把 cancelled 覆盖成 running / completed / failed；现在 `updateTask(..., { onlyActive })` 只改还在 queued / running 的行，标失败 / 进度 / 完成都走它，成片拉到一半发现已取消就把刚落盘的文件删掉。
  - 关页 / 刷新时的软删除本地也同步：以前只 keepalive 发 DELETE，localStorage 里的成片列表没改，刷新回来 `mergeResults` 又把它捞出来；现在 pagehide 直接改 localStorage（成片 + 没剩成片的任务），并给刚删的 id 留 10 分钟「墓碑」——新页面的 `/api/me` 可能比那条 DELETE 先到服务端，合并时按墓碑过滤；进返回缓存再回来的页面撤销条和列表也一起收掉。退出登录前撤销期内的删除立刻提交。
  - 会话失效覆盖到自己解析响应的接口：改密（`/api/auth/change-password`）和建工作流任务遇到 401 / 锁定也广播统一事件回登录页。
  - 偏好同步不再静默丢：推送失败的键放回队列按 3s / 10s / 30s 退避重试（期间用户又改过的键以新值为准），同一时间只有一批在路上；关页前那次推送 keepalive；退出登录前先 `await` 把没推的推掉，不再出现「改完设置 1 秒内点退出 = 这次修改丢了」。
  - 过期成片在简易模式里「加入参考 / 放到画布」禁用并说明原因；放到画布时文件已清理（404）直接报错而不是放一张注定裂掉的图，网络抖动先重试一次；打开画布时把升级前放进去的服务器地址资产后台补转成 data URL（`migrateManagedAssets`），待放图里有一张放不进去不再卡住整批。
  - 「全部推到云盘」和「全部存到本地」一样改成客户端逐张推：N/M 进度条 + 「停止」，WebDAV 没配 / 认证失败这类整批都会挂的错只报一次就停；推完刷新文件管理的总览数字。
  - 测试：`tests/server-shortvideo.mjs` 加「取消 vs 轮询竞争」用例（引擎扣住回包、中途取消、回包说完成也不能把 cancelled 改回去、成片目录不落盘）；`tests/client-review-fixes.mjs` 真浏览器补：后台模板出现在自由创作和创作台的 `/api/generate` 请求体里、撤销期内刷新页面成片不复活（本地 + 服务器）、偏好 PUT 失败后带着同样的键重试、`/api/app-settings` 登录态可拉；`tests/client-free-studio.mjs` 补模式提示插入。
- 第三轮 review 补漏：
  - 缺失的成片 / 视频路径明确 404：以前 `/generated-images/...` 文件被清理后会掉进 SPA 回退，返回 `index.html + 200`，客户端的「过期检测」整个被打穿，画布还会把这页 HTML 转成 `data:text/html` 存进资产（老资产迁移时甚至把原地址永久盖掉）。现在静态目录后面挂了兜底 404；画布拉成片只认 `Content-Type: image/*`，回了别的就按「文件没了」处理，不放进资产。
  - 过期成片的入口补齐：创作台舞台和网格的「加入参考」禁用并说明；画布成片库和画框引用选择器不再列过期成片（简易模式上一轮已禁）。
  - 退出时偏好推不出去不再丢：`flushPreferenceSync({ final: true })` 失败原地再试两次，还不行就把没推出去的改动按账号暂存到设备级的 `clothdesign:unsynced-preferences`（不随退出清账号命名空间一起删，留 24 小时），下次这个账号登录时以它为准落地并补推；掉线、账号切换途中没推出去的同样暂存。三次退避都失败时只放弃这一批的键，请求期间新改的键照常排队（以前会把整个队列一起清掉）。退出最多等 6 秒，服务器僵住也不卡「退出」。
  - 退出 / 关页时删不掉的成片不再复活：墓碑搬到设备级的 `clothdesign:deleted-results`（按账号分组，退出不清），删除先记 `pending`、服务端确认（2xx / 404）后才转成普通墓碑；退出前删除失败原地再试一次，关页那一下只发一次 keepalive；下次登录 `replayPendingDeletes` 补发还没确认的 DELETE。用户当场看到「删除失败」的那种会把墓碑撤掉，不会下次登录偷偷补删。
  - 「全部推到云盘」整批错误按这一张的错误判断（以前永远拿第一条判断，第一张是普通失败、第二张才是认证失败时会继续把剩下几百张都请求一遍）。
  - 短视频回传断流 / 取消把目录清了：`downloadEngineFile` 失败删掉半截的 `.part`，`importFinishedTask` 失败分支连任务目录一起清。
  - 测试：`tests/server-shortvideo.mjs` 加「回传断流」用例（引擎只给半个文件就掐线：任务标失败、目录和 `.part` 都不留）；`tests/client-review-fixes.mjs` 真浏览器补：缺失成片 / 视频路径 404 且不回 HTML、退出时偏好 PUT 全挂 → 原地重试 → 暂存 → 重新登录补推成功并清掉暂存、退出时 DELETE 全挂 → pending 墓碑 → 重新登录补删成功（服务器少一张、列表少一张）。
- 第四轮 review 补漏：
  - 偏好暂存改成「先写后发」：每批偏好在发请求之前就先落到设备级的 `clothdesign:unsynced-preferences`，服务端确认了才划掉（只划同值的键——请求路上用户又改过的新值留着）。以前是「清队列 → 发请求 → 失败回包才暂存」，退出等过了 6 秒超时、请求一直挂着、关页时 keepalive 有没有送到，这三种都等不到失败回包，改动会凭空没了。关页时上一批还挂在路上、排着的这批也先同步落进暂存再发。代价：页面在请求路上关掉时下次打开会把同一个值再推一次（幂等，无害）。
  - 设备级补偿数据（偏好暂存、删除墓碑）写不进 localStorage（配额满 / 隐私模式 / 被禁用）不再假装「已暂存」：新增 `src/lib/durableState.ts`——写失败退到内存（同一页面会话内退出再登录还在）并落一份到 IndexedDB（刷新后 `hydrateDurableState()` 捞回来、再试写回 localStorage，成功就删副本），同时上报；`stashUnsyncedPreferences` / `rememberDeletedResults` 回报有没有真写进去，上报信息按实际情况说。登录 / 启动先等补水完成再落偏好、过滤墓碑、补删。
  - 画布：成片拉不下来（断网 / 超时，重试一次仍失败）改为报错「成片暂时拉取失败，请稍后重试；这张成片仍在成片库里」，不再把 `/generated-images/...` 地址写进画布资产——那种资产没赶上补转，三天后就是一张裂图。`migrateManagedAssets` 只负责升级前留下的老资产。
  - 创作台过期成片的剩余入口：舞台「放大」禁用、「下载」变禁用态（舞台 + 右栏列表）、缩略胶片标「已清理」不再拉失效图、放大层对过期成片不开；任务面板的预览图过期也不拉。
  - 测试：`tests/client-review-fixes.mjs` 真浏览器补：退出时 PUT 一直不回包 → 6 秒内照样退出、暂存里有这份改动、挂着的请求最终失败也不丢、重登补推；模拟 localStorage 对暂存键抛 QuotaExceededError → 退出时 localStorage 里没有、IndexedDB 里有副本 → 刷新 + 登录后从 IndexedDB 补水并补推、副本删掉；`/api/me` 全标过期 → 创作台放大禁用、无下载链接、胶片无 `<img>`；画布成片库点一张拉取被掐断两次 → 提示重试、画布上没有 `<img>`。
- 第五轮 review 补漏（`durableState` 双存储的一致性）：
  - 两边都带时间戳：localStorage 落的是 `{ $durable, at, value }` 信封（老格式裸值按 at=0 读），IndexedDB 副本是 `{ at, value }`；启动补水按时间戳比，副本更新才采用（写回 localStorage），本地更新或一样新就把副本删掉——残留的旧副本不会再盖掉新值（比如把用户后来决定保留的成片当 pending 墓碑补删掉、把旧偏好重新推上服务器）。`idbShadow` 只在 IndexedDB 确认删掉、且之后没再写过新副本时才清，删失败就留着下次写成功再删。
  - IndexedDB 兜底写入不再 fire-and-forget：写失败上报（`IndexedDB 也写不进去，只留在内存里`）；`flushDurableWrites()` 能等到所有未落定的写 / 删，退出前会等它（上限 1.5 秒）再发 sign-out，退出后马上关页也不丢；偏好同步的上报按实际说清「落在 localStorage / 只有 IndexedDB / 只剩内存」。关页（pagehide）那一下仍是尽力而为——卸载中没有可以等的东西。
  - 登录补推的偏好日志不再「读完就删」：`peekUnsyncedPreferences` 只读，日志留到服务端确认同值成功（`clearUnsyncedPreferences`）才划掉；读完、排队 1.5 秒内页面被杀也不会让下次登录被服务端旧值盖掉。
  - 账户页「最近生成」和后台「生成审计」的过期成片显示「已清理」占位，不再去拉 404。
  - 测试：`tests/client-review-fixes.mjs` 真浏览器补：注入比本地旧的 IndexedDB 副本 → 刷新后不补推、不写回、副本被删；注入更新的副本 → 采用、补推、清掉；localStorage 和 IndexedDB 同时写不进去 → 上报两条（IndexedDB 写失败 / 只剩内存）、同一会话内重新登录从内存补推；`/api/me` 全标过期 → 账户页最近生成无 `<img>`。
- 第六轮 review 补漏（两处跨账号串数据 + 退出清理收口）：
  - 画布按账号分库：tldraw 的 `persistenceKey` 带账号 id（`clothdesign-free-canvas:<账号>`），账号 B 登录看不到账号 A 的画布、图片、提示词和标注；换账号画布重挂。退出登录时删掉这个账号的画布库（等画布卸载、tldraw 关掉连接后再删；被别的标签页占着超时就上报，库只对这个账号可见）。升级前所有账号共用一座库：升级后第一个登录的账号把它接过来（整库拷进自己的库、再删旧库，只接一次），不会一刷新画布就空了。「清空本机画布内容」也按账号清。
  - 本地文件夹句柄按账号存（`folder:<账号>`），换账号重新读这个账号的，不再只在 App 挂载时读一次；退出登录时这个账号的句柄和内存引用一起清掉——账号 B 不会接着往账号 A 选的目录里自动写成片。升级前共用的那个句柄同样由升级后第一个登录的账号接管。
  - 退出时 IndexedDB 里的账号数据（简易模式附件等）删除不再 fire-and-forget：`clearStoredStateAccount` 改成 async，退出流程等它（连同文件夹句柄清理）完成再切页（上限 3 秒），删失败 / 超时上报，不吞。
  - 偏好暂存 / 删除墓碑的 TTL 真的删数据：以前读的时候只过滤不写回，过期的提示词 / 草稿 / 表单原文会一直留在 localStorage / IndexedDB 里；现在启动补水后、退出时、以及每小时各清一次，过期的才真的从存储里删掉（没过期的不动、不白写）。
  - 仍是有意的设计：同步失败留下的偏好暂存（≤24 小时）和 pending 墓碑是设备级补偿数据，退出不清——这一点和「退出清掉当前账号全部本地数据」有出入，要不要在退出时让用户选「保留待同步 / 放弃并清理」另议。
  - 测试：`tests/client-review-fixes.mjs` 真浏览器补：账号 A 开画布 → 库名带 A 的 id、没有升级前共用的库；给 A 种一个文件夹句柄 → 文件管理显示；A 退出 → A 的画布库和句柄都没了；种一份升级前共用的旧画布库（`session_state` 标记）和旧句柄 → 账号 B 注册登录 → 旧库内容整库搬进 B 的库、旧库删掉、文件管理显示旧句柄、旧键删掉、A 的没复活 → B 开画布正常 → B 退出 → B 的也清掉；种过期 + 没过期的暂存 / 墓碑 → 刷新后过期的真的从 localStorage 里没了、没过期的还在。
- 第七轮 review 补漏（上一轮两处迁移 / 清理的中断路径）：
  - 旧画布库接管有持久化归属：设备级 `clothdesign:legacy-canvas-migration` 记 `{ accountId, stage }`——先认领（写归属进 localStorage，写不进去就不接）再整库拷（四张表一个事务），拷完记 `stage=delete`，旧库确认删掉才清标记；删不掉（被别的标签页占着）标记留着并上报，下次登录 / 退出再删。别的账号看到有人认领过一律不碰；归属账号退出时旧库跟自己的库一起删。不会再出现「A 拷完没删掉、A 退出、B 登录把旧库接过去」。
  - 旧文件夹句柄接管改成原子：「写到账号键 + 删旧键」在同一个 IndexedDB 读写事务里；账号键和旧键同时存在（老版本标签页后来又写了旧键）时账号键为准、旧键顺手删掉，不会留给下一个账号接。
  - `forgetLocalFolder` 失败上报（不再只返回 false）；退出清理的 `false` 由 `clearStoredStateAccount` / `forgetLocalFolder` 各自报具体原因，`null`（超时）由退出流程报。
  - 退出待清的画布库持久化：退出前先把账号写进设备级 `clothdesign:pending-canvas-purge`，画布卸载后那次 effect 里删，删成功才划掉；启动时（不管登没登录）和任何账号登录挂画布之前都先补删一遍——退出后马上关页也不会让旧画布下次重新冒出来。
  - 测试：`tests/client-review-fixes.mjs` 真浏览器补：旧库归属 A、`stage=delete` 就关页 → B 登录不碰旧库也不多建库、B 退出旧库还在 → A 再登录旧库删掉、标记清掉；归属 A、`stage=copy` → A 再登录整库补拷完再删；A 开着画布时把库里塞内容 + 待清列表写 A → 刷新 → 挂画布前补删（新库里没那份内容）、列表清空；退出后（登录页）种库 + 待清列表 → 刷新 → 启动补删；账号键 + 旧键同时存在 → 账号键为准、旧键删掉。

## V0.8.2 - 2026-08-20

- 增加统一的安全出站请求层：校验域名解析结果，拒绝本机、内网、云元数据等地址，限制重定向并防止跨域转发敏感请求头；参考图、工作流素材和 WebDAV 请求统一接入。
- 外部图片下载增加独立超时、响应体大小上限和像素上限，PNG 解压也受限，避免异常图片或压缩炸弹拖垮服务；上传大小限制与该上限统一。
- API 在认证后才解析受保护接口的大 JSON / multipart 请求，调试无限额度在生产环境显式开启时拒绝启动；修改密码或锁定账号后使旧 Session 失效。
- localStorage 改为按账号命名空间隔离，切换账号时不会串读任务、成片或草稿，退出时清理当前账号的本地数据；补充安全加固、认证和存储隔离测试。

## V0.8.1 - 2026-08-20

- 后台不再是一条从头拉到尾的长页面：左边加了一列分区导航，分成「总览 / 用户 / 图像接口 / 视频模块 / 计费 / 成片与存储」六块，一次只渲染当前这一块。原来十几个版块（运行概览、图像接口、Seedance、短视频、模型路由、积分规则、充值套餐、用户表、支付三块、生成审计、存储策略、提示词模板、积分流水）全摞在一起，改个套餐要滚过整张用户表，Seedance 和短视频的配置也在每次进后台时白拉一遍。
- 分区写进地址栏 hash（`/admin#users`），刷新、前进后退、收藏都停在原处；切换分区自动回到顶部。「用户」那一格上直接标出待开通账号数，不点进去也知道有人在等开通。
- 顺手修「商业化底座」那三条说明：以前挤在半栏里连成一行字（「用户系统Better Auth 管理登录态…」），现在一条一张卡；`.admin-two` 加 `align-items: start`，左右两栏不再被高的那栏拉出一片空白。内容区宽度上限从 1120px 放到 1360px（侧栏 212px + 内容），用户表和接口表格能多喘口气。
- 新增 `tests/client-admin-nav.mjs`：六个分区都在、hash 双向同步、版块一个都没在重排里丢掉或复制两份。

## V0.8.0 - 2026-08-20

- 「短视频」入口下分成两个子模块（沿用「开始创作」的用途药丸）：第一个是新加的「AI 直出」——直接调火山方舟的 Seedance 视频模型出片；第二个是原来的「文案成片」（MoneyPrinterTurbo 拼素材）。两者共用一把权限开关，默认仅 admin。
- 新增 Seedance 模块：文生视频 / 图生视频（首帧 + 可选尾帧）/ 多模态参考（参考图 / 视频 / 音频，`@图像1` 引用；2.5 还能显式指定参考 / 编辑 / 延长）。参数按官方文档列了 7 个模型的能力矩阵（2.5 / 2.0 / 2.0 fast / 2.0 mini / 1.5 pro / 1.0 pro / 1.0 pro fast），界面按所选模型增减：画幅（6 种 + 自动）、分辨率（480p–4K）、时长（秒或智能）、有声直接摆；种子、固定镜头、水印、返回尾帧、帧数、输出格式、服务等级、优先级、样片模式、任务类型、联网搜索、任务超时、一次几条收在「高级参数」里默认收起。不支持的参数不会发给方舟（它的强校验会直接报错）。
- Seedance 素材：上传即按方舟硬限制校验（图 300–6000 px、宽高比 0.4–2.5；视频 2–30 秒、≥ 24 fps；音频 2–30 秒；各自的大小上限），小图 base64 内联、视频 / 音频 / 大图走本站公网地址（`/api/seedance/refs/public/<随机编号>.<ext>`）。成片 24 小时内从方舟拉回本地长期保存，支持 Range 播放；任务卡上可「用尾帧接着拍」「同参数再来一条」「样片出正式版」；排队中可取消，生成中方舟不支持取消要照实说。方舟侧错误（Key 不对、模型未开通、参数不对、安全审核、超时）都翻成能看懂的中文。
- 后台新增「Seedance 接口」：API Key（加密落库、只回脱敏提示）、接口地址、默认模型、每人同时几条、公网地址、开放给用户哪些模型；「测一下」只列任务 + 拉模型列表，不生成、不花钱。`.env` 新增 `SEEDANCE_*`（后台改过的覆盖 `.env`）。
- 新增 `server/seedance.mjs` / `server/seedance-ark.mjs` / `server/seedance-settings.mjs`、`src/components/ShortVideoHub.tsx` / `SeedanceStudio.tsx` / `AdminSeedance.tsx`、`tests/server-seedance.mjs`（假方舟，全链路）、`tests/client-seedance.mjs`，`docs/seedance-module.md`。修 `.env.example` 里一行写坏的 `SHORTVIDEO_LLM_PROVIDER` 注释。
- Seedance 素材输入照作图那边来：图片可以直接拖到首帧 / 中间帧 / 尾帧槽位或参考列表，⌘ / Ctrl + V 粘贴剪贴板里的图直接收进当前槽位，一次选多张按顺序落位；「最近上传」的卡片也能拖到槽位。
- Seedance 图生视频不再只有首帧 / 尾帧，可以加中间帧（首帧 + 中间帧 + 尾帧最多 9 张）。方舟本身只认首尾帧，所以给两种落地法：「一镜到底（参考）」把所有关键帧当参考图交给 2.x，提示词前自动加一段「@图像1 是第一帧…依次经过…@图像N 是最后一帧」，一条任务出一段连续视频、按一条计费；「分段接力（严格）」相邻两张各出一段首尾帧视频（任何支持尾帧的模型都行），超出并发的段在本站排队依次补交，全部完成后用 ffmpeg 自动拼成一条（`seedance_group` 表 + `/api/seedance/groups/:id`，各段照样能单独下载，合并失败可重试；服务器没装 ffmpeg 就照实说）。
- 服务器上的文件保留期统一：上传到服务器的东西（Seedance 参考素材、文案成片的本地素材 / 音乐）最多留 24 小时，到点自动清（还被排队 / 生成中任务引用的素材先留着；文案成片引擎在本机时直接删引擎目录里的文件，引擎自带的歌不动，引擎 `storage/tasks` 里超过 24 小时的旧任务目录也清；成片拉回本站后立刻删引擎侧副本）；生成的视频和生成图同一套：服务器暂存 3 天，到期删文件、记录标 `expired`（任务卡照实说明已清理、取文件回 410），开了 WebDAV 自动归档的账号生成完会自动推云盘，任务卡上也能手动「推到云盘」。清理挂在原来每小时一次的存储巡检里（`registerStorageMaintenanceHook`），后台巡检摘要里能看到各模块清了多少。以前 Seedance 的素材和成片、文案成片的上传和成片都是永久放着的。
- Seedance 的 401 分清楚两种情况：只读接口过不了是 Key 不对；能列任务却在建任务时被方舟回 AuthenticationError，是这把 Key 的权限范围没包含这个模型（方舟 API Key 可限定「全部资源 / 自定义资源」、IP 白名单、所属资源项目，越界时它不会说 ModelNotOpen），提示直接写明去「API Key 管理」查什么。方舟的 401 / 403 不再原样回给浏览器（会被当成掉线），包成 502 并带 `arkCode` / `arkStatus`。后台「测一下」新增模型调用权限自检：对目录里每个模型发一个 content 为空、分辨率非法、注定被参数校验拦下的请求，按方舟先鉴权再校验的顺序判断「可调用 / Key 无权限 / 未开通」，不建任务、不花钱。方舟侧非 2xx 响应记一行日志（状态 / 错误码 / 请求号，不含 Key），便于对着控制台查。
- 修画布上的浮层被压住：右上角的「成片」抽屉、「?」说明和顶栏的「生成任务」面板，选中画布上的东西时会被 tldraw 弹出来的样式面板盖掉半边，连抽屉的关闭按钮都点不到。原因是 tldraw 自带一整层 z-index 300 的 UI（工具栏 / 样式面板），而它给业务用的 `InFrontOfTheCanvas` 被焊死在 250 那层，写在里面的东西 z-index 再大也翻不过去。现在成片抽屉和说明改挂到画布容器根节点上（`CanvasTopLayer`，portal），任务面板和底部全局提示条的层级也一并抬到画布 UI 之上。
- 成片抽屉分页，一页 9 张（3 × 3 正好铺满面板，不用滚），底部显示「共 N 张」和页码。本地最多留 200 张成片，原来一次全铺出来既要滚半天，也要一口气拉 200 个缩略图。
- 新增 `tests/client-canvas-overlays.mjs`：真浏览器里把画框选中让样式面板弹出来，再用 `elementFromPoint` 确认抽屉 / 说明 / 任务面板确实在最上面，并翻一遍成片的页。

## V0.7.1 - 2026-08-19

- 任务卡上显示这次跑了多久：跑的时候是「已跑 12s」，每秒往前走；跑完定格成「用时 34s」。三处任务列表都加了——右上角「生成任务」面板、简易模式的生成中占位卡和右侧预览、短视频任务列表。原来只写了一个提交时刻（「17:36」），进度条又只是个来回滚的动画，等下去完全不知道已经等了几分钟，也没法回头比较哪次慢。
- `GenerationTask` 新增 `startedAt` / `finishedAt` 两个毫秒时间戳：原来的 `createdAt` 是「17:36」这样的显示文案，算不出时长。短视频任务直接用服务端已有的 `createdAt` / `finishedAt`。缺时间戳的旧任务（本地留存的历史、刷新时断了跟踪的那些）不画这一格，不拿两个时钟凑一个看着像真的假数字。
- 新增 `src/lib/duration.ts`（时长文案：`<1s` / `12s` / `2m05s` / `1h02m`，负数夹到 0）和 `src/lib/useNow.ts`（只在有任务在跑时才开每秒定时器，闲着不重画整列）。新增 `tests/client-task-duration.mjs`。

## V0.7.0 - 2026-08-19

- 后台「生成审计」改成 3 列 × 5 行的卡片网格（一页 15 张）。原来一行只放一条，右边三分之二整片空着，一屏看不到几张。
- 后台「积分流水」默认收起并挪到页面最底部，点标题才展开——这块平时不看，却占着首屏最中间的位置。`Section` 随之支持 `collapsible`（带 `aria-expanded`，收起时标题旁显示「共 N 条」）。
- 后台列表和成片列表全部改成服务端分页。原来 `/api/admin/overview` 一次把用户全表 + 订单 / 支付事件 / 积分流水 / 生成审计各 80 条塞在同一个响应里，前端再 `slice(0, 8)` / `slice(0, 12)` 渲染：拉回来的大半是白拉的，而第 13 条往后永远看不到，账号和成片涨到成百上千之后这个响应本身也扛不住。现在 overview 只带每个列表的第一页 + 总数 / 总页数，翻页走各自的 `GET /api/admin/users | orders | payment-events | ledger | generation-results`（`page` / `pageSize`，默认 20、上限 100，页码越界夹回最后一页而不是给一张空白页）。
- 用户列表新增搜索和状态筛选（账号名 / 显示名，待开通 / 已锁定 / 无限额度 / 自备 Key）：账号多了以后靠翻页找人不现实。搜索打到服务端，不是只筛当前页；`LIKE` 里的 `%` 和 `_` 按字面量转义，搜 `a_b` 不会把 `axb` 也带出来。
- 后台用量统计只按当页那几十个账号算。以前每次渲染用户表都要把 `generation_task` / `generated_result` / `credit_ledger` 三张表整个 GROUP BY 一遍，连改一个用户资料都会触发一次全表扫。
- 补上后台要用的全局时间序索引（`payment_order` / `payment_event` / `credit_ledger` / `generated_result` 的 `created_at DESC`、`user_profile` 的 `created_at`）——原来只有 `(user_id, created_at)` 复合索引，跨账号按时间倒序翻页用不上。
- 文件管理的成片列表改成一页 24 张（`GET /api/me/storage?page=`），原来一次拉 300 条整页渲染、每条挂一个 `<img>`。「全部推到云盘 / 全部存到本地」仍然是整个账号的口径：前者用概览里的全量计数，后者会把所有页翻一遍，不会只处理当前这一页。
- 短视频任务列表也分页（原来只给最近 30 条，再往前的看不到）。轮询刷的是当前停留的那一页；「同时最多跑几条」的判断改用服务端给的账号级在跑数，翻到第二页不会把并发上限判错。
- 修 localStorage 会被撑爆而且是静默失败的问题：`clothdesign:tasks` / `clothdesign:results` 以前不封顶地往里堆，而配额只有 5MB 上下，写入失败在 `useStoredState` 里被 `catch {}` 吞掉——表现出来是「刷新之后任务莫名其妙不动了」，从现象根本猜不到是配额问题。现在两者各留最近 200 条（完整历史在服务端），写不进去会先砍掉一半再试并上报一条前端异常。
- 生成任务栏不再一次画上百条：先画 20 条，点「显示更多」再加一批；预览图和提交现场改成按 taskId 建索引查找，原来每条任务都要在成片和提交记录里各 `find` 一遍，两边都上百条时每次渲染都是几万次比较。后台生成审计的缩略图加 `loading="lazy"`。
- 新增 `tests/server-admin-paging.mjs`（真服务器 + 138 个账号 / 137 条订单流水成片：overview 只给一页、翻页不重不漏、越界夹回、`pageSize` 上限、搜索与筛选、`LIKE` 转义、文件管理分页、非管理员 403）和 `tests/client-admin-paging.mjs`（页码窗口是常数级、五个列表都接到分页、不许再用 `slice` 截断代替分页、本地留存上限与配额上报）。

- 新增「短视频」模块（默认仅 admin 可见 / 可用）：一句主题 → AI 写文案 → 抽关键词 → 找素材（Pexels / Pixabay / 本地上传）→ Edge TTS 配音 → 字幕 → 合成 9:16 / 16:9 / 1:1 短视频。渲染交给 [MoneyPrinterTurbo](https://github.com/harry0703/MoneyPrinterTurbo) 的 FastAPI 引擎（只监听本机回环地址），账号、权限、任务表（`shortvideo_task`）、成片落盘、轮询、界面全部在本站；样式沿用整站的纸色 / 金色 token 和 `.simple-card / .chip / .field` 那一套。设计与 API 见 `docs/shortvideo-module.md`。
- 短视频 API：`GET /api/shortvideo/overview`（引擎状态 + 选项目录 + 素材 / 音乐 + 最近任务）、`POST /engine/test`、`POST /script`、`POST /terms`、`POST/GET/DELETE /tasks`、`GET /tasks/:id/files/:name`（带登录、支持 Range）、`GET/POST /materials`、`GET /musics`。文案 / 关键词由本站自己调 OpenAI 兼容 chat 接口生成再交给引擎，引擎侧不用配模型 Key；服务端先规范化 + 校验参数（越界 400、并发上限 429、引擎未接入 503），引擎报完成后立刻把 `final-*.mp4` 和字幕拉回 `SHORTVIDEO_ASSET_DIR`，引擎重启丢任务态也不影响已落盘的成片。
- 权限：`user_profile.shortvideo_enabled`（默认 0）+ 服务端 `canUseShortVideo`（admin 天然可用），账号信息新增 `features.shortVideo`，前端只在为 true 时渲染左栏入口和视图；后台「用户与用量」表新增「短视频」列，`PUT /api/admin/users/:id/shortvideo` 按账号开关。第一版不扣积分。
- 新增环境变量 `SHORTVIDEO_ENGINE_URL / SHORTVIDEO_ENGINE_API_KEY / SHORTVIDEO_ENGINE_TIMEOUT_MS / SHORTVIDEO_ASSET_DIR / SHORTVIDEO_POLL_INTERVAL_MS / SHORTVIDEO_MAX_ACTIVE_PER_USER / SHORTVIDEO_LLM_PROVIDER / SHORTVIDEO_LLM_BASE_URL / SHORTVIDEO_LLM_API_KEY / SHORTVIDEO_LLM_MODEL`（见 `.env.example`）。
- 后台新增「短视频接口」配置区：以前这些只能 ssh 改 `.env` 和引擎的 `config.toml`，界面上根本找不到。现在上半段改本站（文案模型的线路 / 模型名 / 单独 Key / 每人并发 / 渲染线程，存 `app_config`，保存即生效，Key 加密落库只回脱敏提示，还能当场「测一下」），下半段直接读写引擎的 `config.toml`（Pexels / Pixabay / Coverr Key、字幕方案 edge/whisper、引擎并发与队列上限、Azure 与硅基流动的 TTS Key、Whisper 模型和设备），按「节 + 行」定点替换、保留原文件注释，写前自动备份；引擎只在启动时读配置，所以配套一个「重启引擎」按钮（有任务在跑时会拦一下，可强制）。对应 `SHORTVIDEO_ENGINE_CONFIG` / `SHORTVIDEO_ENGINE_RESTART_CMD` / `SHORTVIDEO_RENDER_THREADS`。
- 补齐上游 MoneyPrinterTurbo 里以前漏掉或写死的参数：片段倍速（0.5–2×）、素材跟着文案走（`match_materials_to_script`，会强制顺序拼接）、文案段落数（以前写死 1，现在 1–10）、写文案的额外要求、字幕自定义高度（`subtitle_position=custom` + 距顶百分比，竖屏躲开平台按钮）、字幕底色与圆角、渲染线程数（以前写死 2）、Coverr 素材源、第 9 个字体 UTM Kabel KT。
- 短视频新增「发布文案」：按平台（抖音 / 小红书 / B 站 / TikTok / YouTube Shorts / Instagram Reels）生成标题、简介和话题标签，可一键复制；用本站自己的模型，引擎侧不用配 Key。
- 短视频支持自己上传背景音乐（转发到引擎的 `/musics`，mp3 / m4a / aac / wav / flac / ogg / opus / wma，≤30 MB，引擎会用 ffmpeg 完整解码校验并改成不可变文件名）。
- 短视频界面照实说明两处引擎行为：一次出多条时引擎会强制随机拼接（这时「顺序拼接」不生效）、上传的图片会被转成带缓慢推近的片段。
- 新增 `tests/server-shortvideo.mjs`（真实 Express + 假 MPT 引擎 + 假 chat 接口：权限门禁、文案 / 关键词、创建 → 轮询 → 回传成片 → Range 播放、参数校验、并发上限、引擎失败 / 引擎丢任务、素材上传、删除、后台开关）和 `tests/client-shortvideo.mjs`（入口只在 `features.shortVideo` 分支里、视图守卫、后台开关与配置页、新参数控件、样式只用现有 token）。测试覆盖到引擎配置的定点改写（注释不丢）、Key 不回明文、非管理员 403、发布文案与音乐上传。

## V0.6.0 - 2026-08-18

- 图像接口由单一 URL Base 升级为多供应商并行：保留原 Packy / OpenAI 兼容接口，新增 APIMart（`https://api.apimart.ai/v1`、`gpt-image-2`）；两套地址、模型和共享 Key 独立配置，后台可分别查看、修改和测试。
- 账户页和后台账号管理新增 URL Base 选择。账号自备 Key、站点共享 Key与所选供应商成套解析，避免 Key 和地址串用；切换后立即对后续生成生效。
- 新增 APIMart 专用适配：文生图和参考图都提交到 `/images/generations`，参考图使用 `image_urls`，异步响应自动轮询 `/tasks/{task_id}` 并下载最终图片。
- 自由生成的比例完整支持 `auto` 和 APIMart 文档列出的 15 种比例，并按所选比例实时显示对应输出像素。
- 比例选择器重做：不再用原生 `<select>`（系统菜单里只有一列「3:2 / 2:3 / 3:4」，横竖分不清，聚焦时的描边也和整站对不上）。现在每一项都配一张按真实宽高比画的示意图，并写明这一档实际交付多少像素；选 16:9 实际按 3:2 出图这类落差在选之前就标出来。支持键盘上下选择、Esc 关闭。
- 分辨率按线路能力开放：Packy / OpenAI 兼容接口没有 `resolution` 这个参数、出图恒定是 1024/1536 那一档，所以只开 1K，2K / 4K 留在原地置灰并写明原因；APIMart 才有 1K / 2K / 4K。服务端出图前按同一个上限再裁一刀，老页面或直接打接口也不会按 4K 计费——以前在 Packy 线路上点 4K 会按 1.9 倍扣积分，拿到的却是同一张图。
- 后台可按账号设「分辨率上限」（跟随线路 / 最高 1K / 2K / 4K），只能在线路能力之内往下压，压不上去；账户页的线路说明也带上「最高 xK」和「管理员已压到 xK」。
- 输出像素按线路算：APIMart 用它自己的尺寸表（含 2K / 4K），OpenAI 兼容线路按 `apiSize` 那一档，不再拿 APIMart 的数字去报一个 Packy 出不来的像素。
- 顶栏的接口状态灯登录后自动实测一次（只打 `/models`，不生成图片）：原来靠「最近一次真实出图」推断，自备 Key 的账号一条都不算进去，于是跑了一天图顶栏还写着「未实测」。自动测通静默点亮，测不通照样弹提示。
- 接口测试的提示条会自己退场（成功 6 秒、失败 15 秒），不用非得手动点 ×。
- 比例下拉不再被裁掉半截：简易模式左边那张卡是定高滚动容器，而比例控件就在卡的底部，菜单留在卡里往下弹必然超出边界。现在菜单挂到 `body` 上按剩余空间决定往上还是往下弹，高度也照着可用空间收；同时排成三列，16 个比例一次全看见，不用在小框里再滚一次。
- 去掉重复的「输出像素」高亮带：同一个数字比例选择器上已经写了（按钮副标题和每一个选项），省下的高度让参数区和生成按钮更容易一屏看完。
- 自由创作的描述示例不再预设成服装（原来是「一件米白色羊毛大衣挂在木质衣架上…」），换成中性例子并直说人物 / 产品 / 风景 / 插画 / 海报都行——这一栏本来就不套行业约束。
- 新增 APIMart 异步任务集成测试，覆盖供应商切换、`21:9 + 4K` 参数、任务轮询和成片持久化。
- 新增 `tests/server-resolution-limits.mjs`：真服务器上跑「4K 请求在 Packy 线路被裁回 1K 且不多扣积分」「换 APIMart 后 4K 放开」「后台按账号压上限 / 压不上去 / 清空恢复」。
- 新增 `tests/client-ratio-resolution.mjs`：档位裁剪、上限说明文案、两条线路各自的输出像素，以及比例选择器和上限在各界面的接线。
- 生产构建改为完整 release 构建后原子切换 `dist`，避免部署过程中浏览器拿到缺失的 CSS/JS 或 SPA 回退页。

## V0.5.0 - 2026-08-18

- 修复画布左上角布局：tldraw 默认把菜单条（≡ / Page 1 / 撤销…）焊死在容器角上，正好压掉顶栏那条分隔线和左侧导航栏的边线。现在它像右上角的「成片 / 清空 / ?」一样浮起来，四周留 8px。
- 参考图支持直接粘贴（⌘/Ctrl + V），三处都通：
  - 画布：选中画框后在任意位置粘贴，图片落在画框左边并直接成为这个画框的引用图（原来只有把光标点进描述框才收）。没选画框时仍旧交给 tldraw，粘成普通画布图片。
  - 创作台：粘贴填进第一个空的参考素材位；鼠标停在某张素材卡上粘贴则替换那一张。
  - 简易模式：粘贴直接进附件条，超出上限的部分不收。
- 画布上标注过的图可以直接当参考图用：在画框的「+ 参考图 → 画布上的图」里，带标注的图会多一个「带标注」按钮，点一下就把「原图 + 图上的标注」拍平成一张图挂到画框上（默认按「入画」使用，卡片上有「带标注」角标）。提示词里会额外交代「箭头和文字是修改指令，不是画面内容，成片里不得残留」，所以既能写自己的描述，也能改比例、加别的参考图，跟一步到位的「按标注改图」互补。
- 简易模式提交后左边立刻清空，可以接着写下一张排队生成，不再被「正在生成」锁住整块表单（描述框、附件条、清空按钮都可用；按钮因为描述被清空而自然变灰，顺手挡住了误触双提交）。提交当场失败、而用户还没开始写下一张时，刚才那份描述和参考图会自动放回来。
- 配套的「提交详情」：每次提交都会存下当时的描述、参考图缩略图（长边 128px）和参数（比例 / 输出像素 / 张数 / 画质 / 格式 / 背景 / 参考图保真 / 提交时间），最近 20 次留在本机。
  - 成片右下角原来的「提示词」改成「提交详情」，展开就是这次提交的全部现场，仍可「用这段重做」「复制描述」。
  - 顶栏「任务」面板里每条任务也带上这一行和参考图缩略图 —— 任务还在跑、输入框已经清空时也查得到。
- 生成按钮不再乱跑：状态文案挪到同一行左边、按钮钉死在右上角（以前整块右对齐，文案一变按钮就左右横跳）；附件条留出一张卡的高度，加图 / 删图 / 提交后自动清空都不会让下面的参数和按钮上下弹。张数只在「N 张生成中」上说一次。
- 修复「任务永远停在运行中」：`/api/generate` 出错时，退款和任务落库原本写在同一个 `cost > 0` 分支里，自备 Key 的账号 cost 恒为 0，一失败就整段跳过 —— 后台看着还在跑，其实早断了（线上真发生过：两张连着提交，前一张超时后就此消失）。现在失败一律落库并打日志（`[generate] task xxx failed: …`），退款只在真扣过积分时做。
- 修复上一条改出来的误报：顶栏的「图像接口健康度」是拿最近一条任务算的，重启收口写进去的失败记录会被当成最近一次接口结果，于是整站挂出「图像接口不可用 · 图像接口异常」——接口其实好好的。现在任务记录多一列 `failure_source`（provider / system），健康度只看图像接口自己的失败；升级时把历史的重启收口记录一并补上标记。
- 服务启动时把残留的「运行中」任务收口成失败：进程重启会把手上正在跑的请求一起带走，那些任务永远不会有结果。前端同理，页面刷新后本地任务列表里的「运行中」也会收口，并说明出图成功的话成片仍会同步进来。
- 画布上的图可以直接送进简易模式的参考图：选中一张图 → 图片工具条上的「加到简易参考」；标注过的图还多一个「带标注加到简易」（拍平成一张带标注的图，按「入画」使用）。和原来的「放到画布」正好互为一对。
- 新增 `tests/client-canvas-references.mjs`：真浏览器里跑完整链路——粘贴成参考图、标注后拍平成参考图、画布图送进简易参考、创作台参考素材粘贴、光标在描述框里粘贴。
- 新增 `tests/client-simple-submit.mjs`：提交后清空与继续排队、任务面板与成片里的提交详情、提交失败后把输入放回来。
- `tests/server-generate-timeout.mjs` 补充：自备 Key（不扣积分）的任务失败后也必须标记失败、失败原因落库、重启后残留的运行中任务被收口。

## V0.4.3 - 2026-08-18

- 画布白屏自检改为检查实际 `.tl-canvas` 渲染层，不再把 tldraw 专注模式隐藏工具栏误判为故障；后台标签页和重新回到前台增加合理宽限期。
- 客户端异常日志补充时间戳，持续覆盖画布 chunk 加载、渲染崩溃和白屏自愈现场，并更新生产构建韧性测试。

- 后台只对 `admin`（owner）一个账号开放：服务端 `requireAdmin` 只认 owner，`PATCH /api/admin/users/:id` 不再接受改角色，升级时把早期提成 `admin` 角色的账号一律降回普通用户；客户端普通账号看不到「管理后台」入口，直接敲 `/admin` 会被送回首页；后台用户表的角色下拉框改成只读标签。
- 文件管理重做，去掉一整页假配置（本地缓存上限 / 本地保留小时 / 失败清理小时 / 缩略图留本地都没有对应实现）。现在是三层真实逻辑：
  - **服务器暂存固定 3 天**（写死）：出图落库即「服务器暂存」，带到期时间；服务进程每小时巡检一次，到期删文件、记录标「已清理」（保留标题、时间和云盘备份路径），顺手清孤儿文件。后台「存储策略」能看文件数 / 占用 / 各状态数量和上次巡检结果，可手动巡检。
  - **本地文件夹**：File System Access API 直接写到用户电脑上选定的目录（Chrome / Edge），可勾「每次出图后自动存」，也可逐张 / 全部存本地；按日期分子目录。目录句柄记在这台浏览器里。
  - **WebDAV 云盘**：每个账号自己配地址 / 账号 / 密码 / 目录（密码 AES-GCM 加密落库、不回传），可测试连接、逐张 / 全部推送、勾「每次出图后自动推到云盘」；推送失败会记下原因显示在页面上。创作台右栏「推到云盘」也走这条真实链路，没配时会明确提示。
- 成片状态语义统一：`cloud-temp` = 服务器暂存、`webdav` = 已推云盘、`expired` = 服务器已清理；过期成片在创作台显示占位说明而不是坏图。创作台参数面板里那个「成片自动归档云盘」开关（原来只改本地一个布尔值）移除，自动归档在文件管理里配。
- 新增 `tests/server-storage.mjs`：对着假 WebDAV 服务验证保存配置、测试连接、手动 / 自动归档、失败记录、到期巡检和后台接口权限。
- **修复画布白屏**：画布上只要有带文字的图形，tldraw 默认会等这一页用到的字体全部加载完才渲染编辑器（`maxFontsToLoadBeforeRender: Infinity`）。十几个字体加起来 1MB 出头，只要有一个请求卡住（网络慢、标签页在后台被浏览器压着），画布、工具栏、面板就一个都不出来；它自带的「加载中」遮罩有 0.2s 淡入动画，后台标签页里动画不跑、opacity 停在 0，所以连转圈都看不见，表现就是一片空白，刷新才好。改成 `maxFontsToLoadBeforeRender: 0`：先把画布画出来，字体在后台自己加载（导出走的是另一条路径，会内嵌字体，不受影响）。
- 白屏兜底：
  - 部署新版本后还开着的老页面，点「画布」时会去拉一个已经不存在的 chunk 文件 —— 这在以前会让 React 卸载整棵界面，也就是**整页白屏**。现在 `lazyWithReload` 会自动刷新一次页面拿新文件名，再失败才显示一张说明卡片（可刷新 / 改用简易模式 / 清空本机画布内容）。
  - 全局和画布各加一层错误边界：任何渲染崩溃都显示「哪里坏了 + 能做什么」，不再是一片空白。
  - 画布空白自检：每 5 秒量一次 tldraw 容器，连着两次量不到（容器没了 / 尺寸为 0 / 工具栏没了）就重挂编辑器（画布内容在 IndexedDB 里，不会丢），两次还救不回来就提示刷新。
  - 新增 `POST /api/client-errors`：前端崩溃、未捕获异常和画布空白自检会把一句话 + 调用栈 + 当时的容器尺寸发回服务端，写进服务日志（`journalctl -u clothdesign` 里搜 `[client-error]`），最近 50 条可在 `GET /api/admin/client-errors` 看到（仅 admin）。不带任何图片和提示词内容，按 IP 限速。
- 新增 `tests/client-canvas-resilience.mjs`：跑生产构建，验证 chunk 失效不再整页白屏、画布空白能自愈并上报、上报接口的权限与限流，以及字体请求全部卡死时画布照样渲染。

## V0.4.2 - 2026-08-17

- 新增「自由创作」界面，与创作台共用 `/api/generate`、任务记录和积分记账，但不套服装行业提示词约束。
- 简易模式：一个描述框加一排可选附件，支持多图上传，每张单独标记「参考」（借鉴风格）或「入画」（必须出现在成片里）。
- 无限画布：基于 tldraw v5，自定义 `ai-frame` 形状承载「画框即契约」——写描述、挑引用图、生成后由结果原地替换画框，位置尺寸完全沿用。
- 按标注改图：在图上用画笔/箭头/文字标出要改的地方，导出「原图 + 批注」交给图像引擎，改后的图放在原图右侧，原图与批注一概不动。
- 画布随之获得 tldraw 原生能力：undo/redo、框选多选、绘图与批注工具、导出、IndexedDB 本地持久化。
- 画布里直接取用历史成片：顶部「成片」抽屉列出所有生成过的图，点一下放到画布；正在编辑画框时放进来的图直接成为引用图。
- 按草图生成：在画布上手绘构图再框选，草图和文字标注被当成需求交给图像引擎，草图本身原样保留。
- 「按标注改图 / 按草图生成」统一成一条路径——选中任意内容即可生成，选中里有图片就是改图，纯手绘就是草图，可再补一句说明。
- 画布内置用法说明：空画布自动展开，其余时候点顶部「?」随时调出。
- tldraw 体积较大，改为按需加载：只有真正切到画布时才拉这块 chunk，创作台与简易模式的首包不受影响。
- 明确区分演示模式和真实出图：没有配置 `OPENAI_API_KEY`（或 `OPENAI_DEMO_MODE=true`）时，创作台、自由创作和功能中心都会显示横幅说明「这次点击不会调用图像接口，返回的是占位图」，并给出具体的配置步骤；演示占位图在成片卡、缩略图和画布成片抽屉里都会带「演示」角标。
- 生成过程有了明确反馈：创作台画布显示「正在生成…」并在缩略图轨道占一个位；简易模式结果区先出现占位卡；画布里 AI 画框和「按标注改图 / 按草图生成」都会在结果将要出现的位置显示「生成中」画框（并自动把镜头带过去），完成后由成片原地替换。
- 顶栏任务按钮在有任务运行时显示「生成中 N」并高亮。
- 创作台生成完成后自动切换到新成片，不再停留在上一张。
- 功能中心的任务在服务端异步执行，结果栏新增运行中提示与进度条，运行期间按钮保持禁用，避免重复提交。
- 修复生成响应覆盖 `apiConfig` 时丢失 `providerHealth`，导致顶栏图像服务状态退化成猜测值。
- 成片可以直接加入参考素材：创作台成片工具条新增「加入参考」，优先填当前模式还空着的槽位（原来一律往后追加，「换衣」这类必填模式会一直卡在「还需上传」）；右栏「以此继续」同步改名为「加入参考」；自由创作的大图和历史卡也都有这个入口。
- 新增输出像素回显：生成前就显示这次真正交付的像素（如 1024 × 1024 px），比例选了接口不支持的档位时直接说明实际按什么尺寸出图。
- 新增成片设置锁定：一键把整块参数折叠成只读摘要，锁定期间参数控件和交付预设都不可改，连续出图时不会被误碰；状态记在本地。
- 新增提示词原子 chip：在描述里直接打 `@`（参考图）、`#`（色卡）、`~`（片段）就能唤出选择器，也可以点描述框下面的三个按钮。色卡带十六进制一起插入，色卡和片段都能自己增删；插入会自动补分隔符，选择器开着时回车先落在选项上而不是触发生成。
- 成片可以回看当时的描述：工具条新增「提示词」，展示生成这张图用的原话（不是拼装后的长提示词），支持复制和「用这段重做」；描述会随成片落库，刷新后仍在。
- 自由创作简易模式改成左右分栏：左边写描述和参数，右边固定显示当前成片，历史成片仍在下方，点一下就切到右侧大图。成片时间戳不再显示原始 ISO 串。
- 去掉自由创作工作区里那条标题栏——它只是把顶栏已经写着的「自由创作 / 简易 · 画布」再说一遍；简易/画布切换搬到顶栏，13 寸屏上描述框和成片一屏内看得完。
- 成片展示区改为按容器封顶：原来图片受固定 vh 限制，宽屏上四周全是黑边（只占展示区三分之一），现在短边基本铺满。
- 左侧菜单栏可收起：顶部新增收起/展开按钮，收起后只留 64px 的两字导航条，参考素材栏与会话摘要一并隐藏；状态记在本地，刷新后保持。
- 简易模式点开成片时铺满全屏，不再在图的上下留出没用的空条；关闭键加了底色，压在浅色画面上也看得见。
- 交互模型参考 Cowart（MIT）的「AI 画框 + 就地替换」与「批注驱动改图」，生成层换成本项目自己的图像接口（Cowart 走的是 Codex MCP 消息桥）。
- 无限画布按 Cowart 的操作方式重做：AI 画框变成工具栏里的工具（按 `A` 放在视口中央，或从工具栏直接拖到想要的位置），生成面板固定贴在画框正下方居中，参考图一排 → 描述框 → 生成；参考图可上传、可直接粘贴进描述框、可从画布上的图或成片库里挑，上传/粘贴的图落在画框左侧一列并自动链接，不再靠 shift 多选来「凑」引用图。
- 画布新增「标注」工具（按 `C`）：拖一下拉出红色箭头，松手直接写字，连着标几处不用反复点工具；「按标注改图」搬到选中图片时弹出的图片工具条上，自动收集图周围的箭头、文字和压在图上的画笔圈选，不再对任何选中都弹出一条操作栏；纯手绘选区才显示「按草图生成」。
- 选中画框时右侧样式面板变成「尺寸 / 比例 / 输出像素」：W/H 联动、七个比例预设，画框拖角只缩放不变形；接口按最近原生尺寸交付时成片等比放进画框，不再硬拉伸。画布回到浅色主题，去掉自定义顶栏，成片库 / 清空 / 说明放到右上角。
- 「自由创作」提到导航第一位，登录后直接落在这里。
- 三处新增一键清空：创作台描述框旁的「清空」清描述和参考图（必填槽位保留为空），简易模式卡片头部的「清空」清描述和附件，画布右上角「清空」删掉全部内容且可撤销；成片和设置一律不动。
- 多人使用（VPS 部署）：新注册的账号默认「待开通」，登录后被拦住并说明原因，管理员在后台点「开通」才放行（`SIGNUP_APPROVAL=false` 可恢复注册即用）；每个账号可在「账户」页填自己的图像接口 Key（AES-256-GCM 加密落库，只回显前 3 位和后 4 位），用自备 Key 生成不扣积分，功能中心的生成同样优先走自备 Key；后台用户表新增开通开关、Key 状态和用量汇总（任务数 / 成片数 / 消耗积分 / 30 天 / 自备 Key 次数 / 最近活跃）。

- 「开发调试」入口改为显式开关 `DEBUG_UNLIMITED`：设成 `true` 时 `NODE_ENV=production` 下也保留（内部自用部署），设成 `false` 始终关闭，不设置时维持原来的「仅非生产环境」。
- 调试会话不再共用一个账号：每个浏览器拿到独立的调试座位（cookie 存 12 位随机座位号，有效期从 1 天延长到 180 天），对应一条自己的 `user_profile`，账号名形如「开发调试 · a1b2c3」。此前所有点过「开发调试」的人共用 `__clothdesign_debug__`，会在服务器侧互相看到对方的成片和任务。座位号只认服务端发出的格式，伪造的 cookie 落不到别人的账号上。
- 调试座位也能在「账户」页填自己的图像接口 Key（原来一律禁止），早期那个共用账号仍然禁止。
- 修复：调试座位会被算进「已有账号数」，导致先有人点过「开发调试」时，第一个正式注册的人当不上 owner。

### 图像接口与后台配置

- 图像接口地址和模型名可以在后台改了：`/admin` 新增「图像接口」一节，
  `.env` 里的值作为默认值，后台改过的覆盖值存进 `app_config`，保存即刻生效、不用重启，
  留空保存或点「恢复默认」回到 `.env` 的值。
- 配套加了「测试连接」：用 `GET /v1/models` 验证地址和 Key 是否可用，不产图不花钱。
- 地址会做校验和归一化（必须是 http/https，自动补 `/v1`、吃掉尾斜杠），保存失败时不覆盖原有配置。
- 创作台出图和功能中心的工作流统一走这一份配置，不再各自读 `process.env`。
- 「模型路由」表改名为「模型路由（仅本机备忘）」并加了说明——它只存在浏览器里，
  从来不影响实际出图，之前的标题容易让人误以为改了就生效。
- 修复后台自锁：唯一的管理员在用户表里点一下角色下拉框就可能把自己降成普通用户
  （原生 select 连续触发 change，线上真的发生过），从此谁也进不了 /admin。
  现在服务端会挡住「取消自己的管理员权限」「取消最后一个管理员」「锁定自己」，
  界面上自己那一行的角色和状态也改成只读；改动被服务端拒绝时会提示并把界面回滚，
  不再出现「界面显示改了、数据库其实没改」。
- 登录框不再按模式切换成 `type="email"`——那样后台发的裸账号名会被浏览器校验直接拦下。
- 后台用户表重新排版：显示名改成平时看着像文本、悬停才露出边框的就地编辑框，配上头像和账号名两行；
  余额从「只读输入框」（看着能改其实不能）换成纯文本，无限额度显示 ∞ 并右对齐；
  行高统一、悬停高亮，「改密」不再被挤成两行；无限额度账号的 +100/-100 置灰。
- 「无限」列关闭时显示「关」而不是「开」，「开通」列未开通时显示「待开通」——
  原来的文案分不清是当前状态还是点击后的结果。

### 管理员发号与专属 Key

- 账号体系改为「管理员发号」：对外只有账号名 + 密码，内部仍以 `账号名@clothdesign.local` 存在 better-auth 里；
  登录框接受裸账号名，老的真实邮箱账号照常登录。
- 后台发号表单改成账号名 / 初始密码 / 显示名 / 图像接口 Key / 初始积分 / 无限额度。
  建号时填的 Key 会直接绑到该账号上，对方登录即可用，不需要自己再填一遍。
- 后台发出去的账号固定为普通用户，拿不到 `/admin` 权限；需要额外管理员时在用户表里手动改角色。
- 用户表的「KEY」一格可以点开，随时给某个账号配、换或清除专属 Key。
- 用户列表和账号信息统一显示账号名，不再暴露内部邮箱。

### 注册、额度与用户管理

- 新增自助注册开关 `ALLOW_SELF_SIGNUP`：设成 false 后登录页只剩登录、注册端点直接 403，账号一律由管理员在后台创建。
- 后台可以直接建号：填邮箱、初始密码、角色、初始积分即可，建出来的账号默认已开通，不用再点一次「开通」；也能重置任意账号的登录密码（没有配邮件服务时这是唯一的找回途径）。
- 新增按账号的「无限额度」开关：开了之后该账号生成不扣积分、登录后顶栏显示 ∞。取代原来那个人人可点的调试入口。
- 登录页和顶栏移除「开发调试」入口。调试座位机制保留在服务端（`/api/debug/session`，默认仅非生产环境），但不再有界面入口——要给谁免积分，在后台按账号开「无限」。
- 后台新增「运行概览」置顶：账号数与待开通、今日活跃人数、今日生成与失败数、累计成片、30 天耗分，以及当前是否开放自助注册。
- 用户表新增「无限」开关与「改密」操作，并显示每个账号的邮箱。

## V0.3.0 - 2026-08-14

- 重构主创作台为编辑刊物式工作区，新增左侧参考素材栏、中央深色成片画布和右侧成片设置栏。
- 新增成片预览、缩略图切换、放大、前后对比、画面标注、拖拽上传和参考素材与提示词标记联动。
- 新增新手/专家设置层级、交付预设、提示词建议、快捷键生成、费用明细和生成状态提示。
- 优化模式选择、账户、认证、存储和任务面板的布局与信息层级，并补充管理员入口和会话摘要。
- 更新全局视觉系统、字体和响应式样式，新增当前工作台的独立展示产物。

## V0.2.3 - 2026-07-22

- 完善主工作台、功能中心、账户与素材管理流程，统一当前版本的交互状态和页面布局。
- 增强模式切换、提示词草稿、生成锁定、任务处理、结果操作和工作流参数管理。
- 更新界面样式与展示产物，补充当前版本的核心页面截图和 PDF 汇总。

## V0.2.2 - 2026-06-30

- 优化 AI 功能中心三大模块的结果展示：新增款式方案预览、虚拟模特来源保留度/动态预览和后期处理前后对比。
- 增加结果操作区，支持继续编辑、作为参考、下载、下载全部和同步 WebDAV 等生产交付动作入口。
- 强化上传素材预览，多图上传会显示缩略图网格和数量摘要，减少仅显示文件名造成的操作不确定性。
- 补充工作流 UI 布局验证测试，覆盖模块结构、关键文案、结果卡紧凑模式和对应样式选择器。

## V0.2.1 - 2026-06-30

- 重构 AI 功能中心为顶部模块切换和三栏工作区，上传素材、参数控制、运行状态、当前结果和历史素材更适合连续操作。
- 新增可复用交互控件：单选/多选药丸、可自定义组合框、编号字段卡和数值步进器，用于工作流参数配置。
- 扩展面料到款式参数：新增面料颜色、裂变维度、更多图案、袖长和领型，并将这些控制项写入生成 prompt 与工作流 payload。
- 扩展虚拟模特和后期处理参数：新增更多场景、比例、重色选项和动效预览开关，允许自定义场景/模特/姿势输入进入 prompt。
- 更新工作流 payload 测试，覆盖新增配色、裂变维度和动效预览参数。

## V0.2.0 - 2026-05-22

- 接入自托管账号系统：Better Auth 邮箱密码登录、SQLite session、首个用户/管理员邮箱 owner 权限和 `/admin` 权限保护。
- 新增 SQLite 商业账本：充值套餐、支付订单、支付事件、积分流水、生成任务、生成结果和审计日志。
- 接入支付宝和微信支付底座：支付宝官方 SDK 扫码预下单与通知验签，微信支付 API v3 Native 下单、签名、通知验签和解密；无商户密钥时支持演示支付。
- 将充值和生图扣费移到服务端事务：支付成功幂等入账，生成前扣积分，余额不足拒绝，生成失败自动退款。
- 扩展客户账户页和后台：显示支付二维码、订单状态、积分流水、支付订单、支付事件和人工调分入口。
- 增加支付配置健康检查和关闭演示模式时的启动校验，后台可查看支付宝/微信支付缺失配置。
- 增加 SQLite 在线备份脚本和支付宝/微信支付上线配置文档。
- 新增 AI 功能中心：面料到款式、虚拟模特、后期处理、趋势测款和品牌 DNA 工作流，并展示 live/preview/需外部服务的生产验收状态。
- 图像工作流接入真实 Images API 落盘、尺寸/内容/主体覆盖率质量门、alpha 检测、棋盘格/白底假透明修复，以及虚拟模特本地 MP4 动效预览。
- `/api/generate` 落盘前会按请求的 `size` 对 PNG 结果做中心裁切和下采样归一化；当上游返回 1536x1024 但用户选择 1024x1024 时，交付文件会修正为目标比例和尺寸，过小坏图不会被放大掩盖。
- 生成图片落盘时会按 PNG/JPEG/WebP 文件签名纠正真实 MIME 和扩展名，JPEG/WebP 也会记录宽高、参与低信息/主体覆盖率质量门，并通过 `sharp` 按目标尺寸中心裁切/重编码；重编码会沿用用户选择的压缩/质量参数，避免错误 `content-type`、fallback 或上游比例偏差绕过交付验收。
- 生成图片落盘不再只信任上游 `content-type`；必须识别真实 PNG/JPEG/WebP 签名并读出宽高，否则会拒绝保存，避免文本、截断 PNG 或坏字节被伪装成可交付图片。
- 工作流素材输入也会复用真实图片签名、宽高和最小 256x256 尺寸校验，损坏或过小的 data URL / 远程素材会在本地失败，不再被发送到 image edit、分割、视频或品牌训练服务。
- 功能中心后期处理会优先复用主工作台最近通过质量门的真实生成图，并允许 `/generated-images/...` 受管图片作为 workflow image edit 素材输入；无真实结果时才回退演示素材。
- 功能中心默认一键后期现在覆盖智能抠图、补光增强、手部修复、对象擦除、智能重色和比例调整；后端无 actions 输入时也使用同一完整默认动作集合。
- 面料到款式的受控编辑证据现在会记录并展示一键切换后的面料图案，真实 image edit prompt 也会带入该图案切换说明。
- 虚拟模特的场景动作证据现在会展示“行走/站立/转身”等中文姿势标签，并把同一标签写入真实上身图 prompt。
- 虚拟模特画像证据和真实上身图 prompt 现在会展示“东亚/儿童/标准/女”等中文画像标签，避免把内部枚举暴露给用户或模型。
- 后期智能重色现在会在批量结果证据和真实 image edit prompt 中使用“象牙白/鼠尾草绿/保留原色”等中文商业色名。
- 生产验收范围收敛到基础三模块；真实视频、专用分割服务和品牌训练服务改为可选增强，不再计入阻断项，smoke 会验证基础流程阻断项为 0。
- 原版 `/api/generate` 的参考图输入同样会校验真实图片签名和宽高；坏的 data URL、远程参考图、受管生成图或上传文件会在扣费和调用图像引擎前返回 400。
- 原版 `/api/generate` 会在扣费和调用图像引擎前拦截尺寸低于 256x256 的参考图，避免 1x1 占位素材进入 image edit 后被上游拒绝。
- `/api/generate` 在读取参考图、创建任务和扣费前校验提示词，空提示词会直接返回 400，避免无效生成进入图像引擎或产生扣费/退款噪音。
- `/api/generate` 会拒绝图像引擎 200 响应但 `data` 为空的情况，并触发失败退款，避免任务成功、积分已扣但没有任何可交付图片。
- 图像引擎请求、返回 URL 图片下载、工作流素材下载、视频服务、分割服务、品牌训练服务和真实支付预下单新增超时保护；普通生图超时会让任务失败并自动退回本次扣费，外部服务超时会明确失败，避免请求长期悬挂。
- 工作流外部服务失败后会把失败原因、下一步处理建议和不可交付标记写入任务、步骤和结果元数据；前端功能中心会显示“工作流未通过生产验收”和失败结果卡，避免把占位图误当成可交付结果。
- Live 工作流创建后会保持 running，只有真实图片物化并写入质量门证据后才切换 success，避免短时间出现“成功任务 + SVG 占位结果”的误导状态。
- 普通生图结果会返回 `imageInspection` 和 `qualityGate`，主工作台结果卡显示“质量通过/需返工”和归一化或返工原因，避免尺寸异常、低信息或主体过少的坏图被用户当成可直接交付。
- 普通生图生成结果会把 `imageInspection` / `qualityGate` 持久化到 `generated_result.metadata_json`，避免刷新或后台审计时丢失质量门证据。
- `/api/me` 和后台概览会返回最近生成结果及质量门元数据，账户页和后台审计区可直接查看普通生图的可交付状态。
- 前端生成成功后会把任务轨和结果卡切换到服务端返回的真实 `taskId`，避免本地任务、账户历史和后台审计出现 ID 分叉。
- 结果卡 WebDAV 操作会通过服务端持久化 `storage_status=webdav`，刷新、账户历史和后台审计不再回退到本地缓存状态。
- 结果卡删除会调用服务端删除接口，校验归属后移除 `generated_result` 记录，并在无其他引用时删除受管本地图片文件。
- 删除结果时会同步移除由“继续”加入的同图参考图，避免下一次生成携带已删除的受管图片路径。
- 删除某个任务的最后一张结果时会同步移除任务轨记录，避免保留没有可查看图片的成功任务。
- 图片清理和结果删除会把 `workflow_asset.source_url` 纳入引用保护，避免工作流素材仍在使用的受管生成图被误删。
- 结果下载和“继续作为参考图”会按真实图片 URL / data URL 推断 PNG、JPG 或 WebP 扩展名，不再把真实生成图片误命名为 `.svg`。
- 生成结果“继续”作为参考图时保留受管 `/generated-images/...` 路径，提示词会映射为上传图片，服务端会安全读取本地生成图并作为 image edit 输入；仅携带 `sourceUrl` 的参考图也会计入积分估算，避免只计费未传图或传图未扣费。
- 虚拟模特短视频新增 `AI_VIDEO_API_URL` / `AI_VIDEO_API_KEY` 外部视频服务适配，支持 JSON `url`/`b64_video` 和直接 `video/mp4` 返回；未配置时保留本地 MP4 动效预览。
- 外部视频服务请求会随表单上传真实上身图文件，避免只传本地相对路径导致服务端无法取图；返回视频会校验 MP4 MIME 和 `ftyp` 文件头，避免 WebM 或坏字节被误存为 `.mp4`。
- 后期抠图新增 `SEGMENTATION_API_URL` / `SEGMENTATION_API_KEY` 专用分割服务适配，支持 JSON `url`/`b64_json` 和直接 `image/png` 返回，并继续用 alpha 质量门验收。
- 品牌专属模型新增 `BRAND_TRAINING_API_URL` / `BRAND_TRAINING_API_KEY` 外部训练服务适配，提交品牌素材和 DNA JSON，并回写训练任务 ID、模型 ID 与状态。
- 品牌训练服务即使返回 HTTP 200，也会拦截 `failed/error/canceled/rejected` 等失败状态；任务失败落库时会同步把关联品牌 DNA 档案标记为 failed，避免失败训练继续计入可用品牌资产。
- 生产验收面板显示真实视频、像素级抠图、品牌训练所需服务和环境变量；这些服务未配置时作为可选增强展示，不计入基础三模块阻断项；外部服务配置后，对应能力标签会动态切换为真实接入；质量门返工/复核状态会同步到任务消息。
- 功能中心摘要改为数据库聚合总数，不再被最近 40 条任务或 20 条品牌档案列表截断；训练中的品牌 DNA 也会计入品牌档案摘要；dashboard 加载前显示占位，避免把未知数据误显示为 0。
- 认证 origin 支持临时端口和 `.env` 固定 URL 不一致的本地验证场景。
- 更新 README、商用就绪说明、环境变量样例、服务端测试和 smoke 测试，覆盖登录、模拟支付入账、支付配置校验、通知幂等、金额校验、扣费和后台支付管理。

## V0.1.1 - 2026-05-14

- 将管理员后台拆分为独立 `/admin` 入口，客户页不再暴露后台导航和用户管理能力。
- 增强后台配置能力，支持维护模型路由、积分规则、充值套餐、用户额度、存储/WebDAV 策略和系统提示词模板。
- 优化客户工作台体验：参考图支持拖拽，生成结果改为右侧主图加缩略图，任务系统改为顶部弹层并展示结果缩略图。
- 调整提示词优化流程，前台仅展示可编辑用户提示词，系统提示词和参考图映射在生成时内部拼接。
- 更新 README、商用就绪说明、需求验收清单和 smoke 脚本，覆盖独立后台、前台隐藏系统提示词和任务弹层流程。

## V0.1.0 - 2026-05-13

- 发布 ClothDesign AI 商业化 MVP：单页服装 AI 生图工作台，包含模式选择、提示词、A/B/C 参考图、参数配置、任务轨和结果画廊。
- 增加 Node 服务端 `/api/generate` 代理，默认端口 `8888`，API Key 仅由服务端读取；无 Key 时自动进入演示模式。
- 增加后台模型路由、账户充值与积分、存储生命周期和 WebDAV 策略等商业化管理界面。
- 增加需求完成清单、商用就绪说明、TypeScript/Vite 构建配置和 smoke 自动化验证脚本。

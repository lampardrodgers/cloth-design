# Changelog

## 未发布

- 新增「自由创作」界面，与创作台共用 `/api/generate`、任务记录和积分记账，但不套服装行业提示词约束。
- 简易模式：一个描述框加一排可选附件，支持多图上传，每张单独标记「参考」（借鉴风格）或「入画」（必须出现在成片里）。
- 无限画布：基于 tldraw v5，自定义 `ai-frame` 形状承载「画框即契约」——写描述、挑引用图、生成后由结果原地替换画框，位置尺寸完全沿用。
- 按标注改图：在图上用画笔/箭头/文字标出要改的地方，导出「原图 + 批注」交给图像引擎，改后的图放在原图右侧，原图与批注一概不动。
- 画布随之获得 tldraw 原生能力：undo/redo、框选多选、绘图与批注工具、导出、IndexedDB 本地持久化。
- 画布里直接取用历史成片：顶部「成片」抽屉列出所有生成过的图，点一下放到画布；正在编辑画框时放进来的图直接成为引用图。
- 按草图生成：在画布上手绘构图再框选，草图和文字标注被当成需求交给图像引擎，草图本身原样保留。
- 「按标注改图 / 按草图生成」统一成一条路径——选中任意内容即可生成，选中里有图片就是改图，纯手绘就是草图，可再补一句说明。
- 画布内置用法说明：空画布自动展开，其余时候点顶部「?」随时调出。
- tldraw 体积较大，改为按需加载：只有真正切到画布时才拉这块 chunk，创作台与简易模式的首包不受影响。
- 明确区分演示模式和真实出图：没有配置 `OPENAI_API_KEY`（或 `OPENAI_DEMO_MODE=true`）时，创作台、自由创作和功能中心都会显示横幅说明「这次点击不会调用图像接口，返回的是占位图」，并给出具体的配置步骤；演示占位图在成片卡、缩略图和画布成片抽屉里都会带「演示」角标。
- 生成过程有了明确反馈：创作台画布显示「正在生成…」并在缩略图轨道占一个位；简易模式结果区先出现占位卡；画布里 AI 画框和「按标注改图 / 按草图生成」都会在结果将要出现的位置显示「生成中」画框（并自动把镜头带过去），完成后由成片原地替换。
- 顶栏任务按钮在有任务运行时显示「生成中 N」并高亮。
- 创作台生成完成后自动切换到新成片，不再停留在上一张。
- 功能中心的任务在服务端异步执行，结果栏新增运行中提示与进度条，运行期间按钮保持禁用，避免重复提交。
- 修复生成响应覆盖 `apiConfig` 时丢失 `providerHealth`，导致顶栏图像服务状态退化成猜测值。
- 成片可以直接加入参考素材：创作台成片工具条新增「加入参考」，优先填当前模式还空着的槽位（原来一律往后追加，「换衣」这类必填模式会一直卡在「还需上传」）；右栏「以此继续」同步改名为「加入参考」；自由创作的大图和历史卡也都有这个入口。
- 新增输出像素回显：生成前就显示这次真正交付的像素（如 1024 × 1024 px），比例选了接口不支持的档位时直接说明实际按什么尺寸出图。
- 新增成片设置锁定：一键把整块参数折叠成只读摘要，锁定期间参数控件和交付预设都不可改，连续出图时不会被误碰；状态记在本地。
- 新增提示词原子 chip：在描述里直接打 `@`（参考图）、`#`（色卡）、`~`（片段）就能唤出选择器，也可以点描述框下面的三个按钮。色卡带十六进制一起插入，色卡和片段都能自己增删；插入会自动补分隔符，选择器开着时回车先落在选项上而不是触发生成。
- 成片可以回看当时的描述：工具条新增「提示词」，展示生成这张图用的原话（不是拼装后的长提示词），支持复制和「用这段重做」；描述会随成片落库，刷新后仍在。
- 自由创作简易模式改成左右分栏：左边写描述和参数，右边固定显示当前成片，历史成片仍在下方，点一下就切到右侧大图。成片时间戳不再显示原始 ISO 串。
- 去掉自由创作工作区里那条标题栏——它只是把顶栏已经写着的「自由创作 / 简易 · 画布」再说一遍；简易/画布切换搬到顶栏，13 寸屏上描述框和成片一屏内看得完。
- 成片展示区改为按容器封顶：原来图片受固定 vh 限制，宽屏上四周全是黑边（只占展示区三分之一），现在短边基本铺满。
- 左侧菜单栏可收起：顶部新增收起/展开按钮，收起后只留 64px 的两字导航条，参考素材栏与会话摘要一并隐藏；状态记在本地，刷新后保持。
- 简易模式点开成片时铺满全屏，不再在图的上下留出没用的空条；关闭键加了底色，压在浅色画面上也看得见。
- 交互模型参考 Cowart（MIT）的「AI 画框 + 就地替换」与「批注驱动改图」，生成层换成本项目自己的图像接口（Cowart 走的是 Codex MCP 消息桥）。
- 无限画布按 Cowart 的操作方式重做：AI 画框变成工具栏里的工具（按 `A` 放在视口中央，或从工具栏直接拖到想要的位置），生成面板固定贴在画框正下方居中，参考图一排 → 描述框 → 生成；参考图可上传、可直接粘贴进描述框、可从画布上的图或成片库里挑，上传/粘贴的图落在画框左侧一列并自动链接，不再靠 shift 多选来「凑」引用图。
- 画布新增「标注」工具（按 `C`）：拖一下拉出红色箭头，松手直接写字，连着标几处不用反复点工具；「按标注改图」搬到选中图片时弹出的图片工具条上，自动收集图周围的箭头、文字和压在图上的画笔圈选，不再对任何选中都弹出一条操作栏；纯手绘选区才显示「按草图生成」。
- 选中画框时右侧样式面板变成「尺寸 / 比例 / 输出像素」：W/H 联动、七个比例预设，画框拖角只缩放不变形；接口按最近原生尺寸交付时成片等比放进画框，不再硬拉伸。画布回到浅色主题，去掉自定义顶栏，成片库 / 清空 / 说明放到右上角。
- 「自由创作」提到导航第一位，登录后直接落在这里。
- 三处新增一键清空：创作台描述框旁的「清空」清描述和参考图（必填槽位保留为空），简易模式卡片头部的「清空」清描述和附件，画布右上角「清空」删掉全部内容且可撤销；成片和设置一律不动。
- 多人使用（VPS 部署）：新注册的账号默认「待开通」，登录后被拦住并说明原因，管理员在后台点「开通」才放行（`SIGNUP_APPROVAL=false` 可恢复注册即用）；每个账号可在「账户」页填自己的图像接口 Key（AES-256-GCM 加密落库，只回显前 3 位和后 4 位），用自备 Key 生成不扣积分，功能中心的生成同样优先走自备 Key；后台用户表新增开通开关、Key 状态和用量汇总（任务数 / 成片数 / 消耗积分 / 30 天 / 自备 Key 次数 / 最近活跃）。

- 「开发调试」入口改为显式开关 `DEBUG_UNLIMITED`：设成 `true` 时 `NODE_ENV=production` 下也保留（内部自用部署），设成 `false` 始终关闭，不设置时维持原来的「仅非生产环境」。
- 调试会话不再共用一个账号：每个浏览器拿到独立的调试座位（cookie 存 12 位随机座位号，有效期从 1 天延长到 180 天），对应一条自己的 `user_profile`，账号名形如「开发调试 · a1b2c3」。此前所有点过「开发调试」的人共用 `__clothdesign_debug__`，会在服务器侧互相看到对方的成片和任务。座位号只认服务端发出的格式，伪造的 cookie 落不到别人的账号上。
- 调试座位也能在「账户」页填自己的图像接口 Key（原来一律禁止），早期那个共用账号仍然禁止。
- 修复：调试座位会被算进「已有账号数」，导致先有人点过「开发调试」时，第一个正式注册的人当不上 owner。

## V0.4.3 - 2026-08-17

- 图像接口地址和模型名可以在后台改了：`/admin` 新增「图像接口」一节，
  `.env` 里的值作为默认值，后台改过的覆盖值存进 `app_config`，保存即刻生效、不用重启，
  留空保存或点「恢复默认」回到 `.env` 的值。
- 配套加了「测试连接」：用 `GET /v1/models` 验证地址和 Key 是否可用，不产图不花钱。
- 地址会做校验和归一化（必须是 http/https，自动补 `/v1`、吃掉尾斜杠），保存失败时不覆盖原有配置。
- 创作台出图和功能中心的工作流统一走这一份配置，不再各自读 `process.env`。
- 「模型路由」表改名为「模型路由（仅本机备忘）」并加了说明——它只存在浏览器里，
  从来不影响实际出图，之前的标题容易让人误以为改了就生效。
- 修复后台自锁：唯一的管理员在用户表里点一下角色下拉框就可能把自己降成普通用户
  （原生 select 连续触发 change，线上真的发生过），从此谁也进不了 /admin。
  现在服务端会挡住「取消自己的管理员权限」「取消最后一个管理员」「锁定自己」，
  界面上自己那一行的角色和状态也改成只读；改动被服务端拒绝时会提示并把界面回滚，
  不再出现「界面显示改了、数据库其实没改」。
- 登录框不再按模式切换成 `type="email"`——那样后台发的裸账号名会被浏览器校验直接拦下。

## V0.4.2 - 2026-08-17

- 账号体系改为「管理员发号」：对外只有账号名 + 密码，内部仍以 `账号名@clothdesign.local` 存在 better-auth 里；
  登录框接受裸账号名，老的真实邮箱账号照常登录。
- 后台发号表单改成账号名 / 初始密码 / 显示名 / 图像接口 Key / 初始积分 / 无限额度。
  建号时填的 Key 会直接绑到该账号上，对方登录即可用，不需要自己再填一遍。
- 后台发出去的账号固定为普通用户，拿不到 `/admin` 权限；需要额外管理员时在用户表里手动改角色。
- 用户表的「KEY」一格可以点开，随时给某个账号配、换或清除专属 Key。
- 用户列表和账号信息统一显示账号名，不再暴露内部邮箱。

## V0.4.1 - 2026-08-17

- 新增自助注册开关 `ALLOW_SELF_SIGNUP`：设成 false 后登录页只剩登录、注册端点直接 403，账号一律由管理员在后台创建。
- 后台可以直接建号：填邮箱、初始密码、角色、初始积分即可，建出来的账号默认已开通，不用再点一次「开通」；也能重置任意账号的登录密码（没有配邮件服务时这是唯一的找回途径）。
- 新增按账号的「无限额度」开关：开了之后该账号生成不扣积分、登录后顶栏显示 ∞。取代原来那个人人可点的调试入口。
- 登录页和顶栏移除「开发调试」入口。调试座位机制保留在服务端（`/api/debug/session`，默认仅非生产环境），但不再有界面入口——要给谁免积分，在后台按账号开「无限」。
- 后台新增「运行概览」置顶：账号数与待开通、今日活跃人数、今日生成与失败数、累计成片、30 天耗分，以及当前是否开放自助注册。
- 用户表新增「无限」开关与「改密」操作，并显示每个账号的邮箱。

## V0.3.0 - 2026-08-14

- 重构主创作台为编辑刊物式工作区，新增左侧参考素材栏、中央深色成片画布和右侧成片设置栏。
- 新增成片预览、缩略图切换、放大、前后对比、画面标注、拖拽上传和参考素材与提示词标记联动。
- 新增新手/专家设置层级、交付预设、提示词建议、快捷键生成、费用明细和生成状态提示。
- 优化模式选择、账户、认证、存储和任务面板的布局与信息层级，并补充管理员入口和会话摘要。
- 更新全局视觉系统、字体和响应式样式，新增当前工作台的独立展示产物。

## V0.2.3 - 2026-07-22

- 完善主工作台、功能中心、账户与素材管理流程，统一当前版本的交互状态和页面布局。
- 增强模式切换、提示词草稿、生成锁定、任务处理、结果操作和工作流参数管理。
- 更新界面样式与展示产物，补充当前版本的核心页面截图和 PDF 汇总。

## V0.2.2 - 2026-06-30

- 优化 AI 功能中心三大模块的结果展示：新增款式方案预览、虚拟模特来源保留度/动态预览和后期处理前后对比。
- 增加结果操作区，支持继续编辑、作为参考、下载、下载全部和同步 WebDAV 等生产交付动作入口。
- 强化上传素材预览，多图上传会显示缩略图网格和数量摘要，减少仅显示文件名造成的操作不确定性。
- 补充工作流 UI 布局验证测试，覆盖模块结构、关键文案、结果卡紧凑模式和对应样式选择器。

## V0.2.1 - 2026-06-30

- 重构 AI 功能中心为顶部模块切换和三栏工作区，上传素材、参数控制、运行状态、当前结果和历史素材更适合连续操作。
- 新增可复用交互控件：单选/多选药丸、可自定义组合框、编号字段卡和数值步进器，用于工作流参数配置。
- 扩展面料到款式参数：新增面料颜色、裂变维度、更多图案、袖长和领型，并将这些控制项写入生成 prompt 与工作流 payload。
- 扩展虚拟模特和后期处理参数：新增更多场景、比例、重色选项和动效预览开关，允许自定义场景/模特/姿势输入进入 prompt。
- 更新工作流 payload 测试，覆盖新增配色、裂变维度和动效预览参数。

## V0.2.0 - 2026-05-22

- 接入自托管账号系统：Better Auth 邮箱密码登录、SQLite session、首个用户/管理员邮箱 owner 权限和 `/admin` 权限保护。
- 新增 SQLite 商业账本：充值套餐、支付订单、支付事件、积分流水、生成任务、生成结果和审计日志。
- 接入支付宝和微信支付底座：支付宝官方 SDK 扫码预下单与通知验签，微信支付 API v3 Native 下单、签名、通知验签和解密；无商户密钥时支持演示支付。
- 将充值和生图扣费移到服务端事务：支付成功幂等入账，生成前扣积分，余额不足拒绝，生成失败自动退款。
- 扩展客户账户页和后台：显示支付二维码、订单状态、积分流水、支付订单、支付事件和人工调分入口。
- 增加支付配置健康检查和关闭演示模式时的启动校验，后台可查看支付宝/微信支付缺失配置。
- 增加 SQLite 在线备份脚本和支付宝/微信支付上线配置文档。
- 新增 AI 功能中心：面料到款式、虚拟模特、后期处理、趋势测款和品牌 DNA 工作流，并展示 live/preview/需外部服务的生产验收状态。
- 图像工作流接入真实 Images API 落盘、尺寸/内容/主体覆盖率质量门、alpha 检测、棋盘格/白底假透明修复，以及虚拟模特本地 MP4 动效预览。
- `/api/generate` 落盘前会按请求的 `size` 对 PNG 结果做中心裁切和下采样归一化；当上游返回 1536x1024 但用户选择 1024x1024 时，交付文件会修正为目标比例和尺寸，过小坏图不会被放大掩盖。
- 生成图片落盘时会按 PNG/JPEG/WebP 文件签名纠正真实 MIME 和扩展名，JPEG/WebP 也会记录宽高、参与低信息/主体覆盖率质量门，并通过 `sharp` 按目标尺寸中心裁切/重编码；重编码会沿用用户选择的压缩/质量参数，避免错误 `content-type`、fallback 或上游比例偏差绕过交付验收。
- 生成图片落盘不再只信任上游 `content-type`；必须识别真实 PNG/JPEG/WebP 签名并读出宽高，否则会拒绝保存，避免文本、截断 PNG 或坏字节被伪装成可交付图片。
- 工作流素材输入也会复用真实图片签名、宽高和最小 256x256 尺寸校验，损坏或过小的 data URL / 远程素材会在本地失败，不再被发送到 image edit、分割、视频或品牌训练服务。
- 功能中心后期处理会优先复用主工作台最近通过质量门的真实生成图，并允许 `/generated-images/...` 受管图片作为 workflow image edit 素材输入；无真实结果时才回退演示素材。
- 功能中心默认一键后期现在覆盖智能抠图、补光增强、手部修复、对象擦除、智能重色和比例调整；后端无 actions 输入时也使用同一完整默认动作集合。
- 面料到款式的受控编辑证据现在会记录并展示一键切换后的面料图案，真实 image edit prompt 也会带入该图案切换说明。
- 虚拟模特的场景动作证据现在会展示“行走/站立/转身”等中文姿势标签，并把同一标签写入真实上身图 prompt。
- 虚拟模特画像证据和真实上身图 prompt 现在会展示“东亚/儿童/标准/女”等中文画像标签，避免把内部枚举暴露给用户或模型。
- 后期智能重色现在会在批量结果证据和真实 image edit prompt 中使用“象牙白/鼠尾草绿/保留原色”等中文商业色名。
- 生产验收范围收敛到基础三模块；真实视频、专用分割服务和品牌训练服务改为可选增强，不再计入阻断项，smoke 会验证基础流程阻断项为 0。
- 原版 `/api/generate` 的参考图输入同样会校验真实图片签名和宽高；坏的 data URL、远程参考图、受管生成图或上传文件会在扣费和调用图像引擎前返回 400。
- 原版 `/api/generate` 会在扣费和调用图像引擎前拦截尺寸低于 256x256 的参考图，避免 1x1 占位素材进入 image edit 后被上游拒绝。
- `/api/generate` 在读取参考图、创建任务和扣费前校验提示词，空提示词会直接返回 400，避免无效生成进入图像引擎或产生扣费/退款噪音。
- `/api/generate` 会拒绝图像引擎 200 响应但 `data` 为空的情况，并触发失败退款，避免任务成功、积分已扣但没有任何可交付图片。
- 图像引擎请求、返回 URL 图片下载、工作流素材下载、视频服务、分割服务、品牌训练服务和真实支付预下单新增超时保护；普通生图超时会让任务失败并自动退回本次扣费，外部服务超时会明确失败，避免请求长期悬挂。
- 工作流外部服务失败后会把失败原因、下一步处理建议和不可交付标记写入任务、步骤和结果元数据；前端功能中心会显示“工作流未通过生产验收”和失败结果卡，避免把占位图误当成可交付结果。
- Live 工作流创建后会保持 running，只有真实图片物化并写入质量门证据后才切换 success，避免短时间出现“成功任务 + SVG 占位结果”的误导状态。
- 普通生图结果会返回 `imageInspection` 和 `qualityGate`，主工作台结果卡显示“质量通过/需返工”和归一化或返工原因，避免尺寸异常、低信息或主体过少的坏图被用户当成可直接交付。
- 普通生图生成结果会把 `imageInspection` / `qualityGate` 持久化到 `generated_result.metadata_json`，避免刷新或后台审计时丢失质量门证据。
- `/api/me` 和后台概览会返回最近生成结果及质量门元数据，账户页和后台审计区可直接查看普通生图的可交付状态。
- 前端生成成功后会把任务轨和结果卡切换到服务端返回的真实 `taskId`，避免本地任务、账户历史和后台审计出现 ID 分叉。
- 结果卡 WebDAV 操作会通过服务端持久化 `storage_status=webdav`，刷新、账户历史和后台审计不再回退到本地缓存状态。
- 结果卡删除会调用服务端删除接口，校验归属后移除 `generated_result` 记录，并在无其他引用时删除受管本地图片文件。
- 删除结果时会同步移除由“继续”加入的同图参考图，避免下一次生成携带已删除的受管图片路径。
- 删除某个任务的最后一张结果时会同步移除任务轨记录，避免保留没有可查看图片的成功任务。
- 图片清理和结果删除会把 `workflow_asset.source_url` 纳入引用保护，避免工作流素材仍在使用的受管生成图被误删。
- 结果下载和“继续作为参考图”会按真实图片 URL / data URL 推断 PNG、JPG 或 WebP 扩展名，不再把真实生成图片误命名为 `.svg`。
- 生成结果“继续”作为参考图时保留受管 `/generated-images/...` 路径，提示词会映射为上传图片，服务端会安全读取本地生成图并作为 image edit 输入；仅携带 `sourceUrl` 的参考图也会计入积分估算，避免只计费未传图或传图未扣费。
- 虚拟模特短视频新增 `AI_VIDEO_API_URL` / `AI_VIDEO_API_KEY` 外部视频服务适配，支持 JSON `url`/`b64_video` 和直接 `video/mp4` 返回；未配置时保留本地 MP4 动效预览。
- 外部视频服务请求会随表单上传真实上身图文件，避免只传本地相对路径导致服务端无法取图；返回视频会校验 MP4 MIME 和 `ftyp` 文件头，避免 WebM 或坏字节被误存为 `.mp4`。
- 后期抠图新增 `SEGMENTATION_API_URL` / `SEGMENTATION_API_KEY` 专用分割服务适配，支持 JSON `url`/`b64_json` 和直接 `image/png` 返回，并继续用 alpha 质量门验收。
- 品牌专属模型新增 `BRAND_TRAINING_API_URL` / `BRAND_TRAINING_API_KEY` 外部训练服务适配，提交品牌素材和 DNA JSON，并回写训练任务 ID、模型 ID 与状态。
- 品牌训练服务即使返回 HTTP 200，也会拦截 `failed/error/canceled/rejected` 等失败状态；任务失败落库时会同步把关联品牌 DNA 档案标记为 failed，避免失败训练继续计入可用品牌资产。
- 生产验收面板显示真实视频、像素级抠图、品牌训练所需服务和环境变量；这些服务未配置时作为可选增强展示，不计入基础三模块阻断项；外部服务配置后，对应能力标签会动态切换为真实接入；质量门返工/复核状态会同步到任务消息。
- 功能中心摘要改为数据库聚合总数，不再被最近 40 条任务或 20 条品牌档案列表截断；训练中的品牌 DNA 也会计入品牌档案摘要；dashboard 加载前显示占位，避免把未知数据误显示为 0。
- 认证 origin 支持临时端口和 `.env` 固定 URL 不一致的本地验证场景。
- 更新 README、商用就绪说明、环境变量样例、服务端测试和 smoke 测试，覆盖登录、模拟支付入账、支付配置校验、通知幂等、金额校验、扣费和后台支付管理。

## V0.1.1 - 2026-05-14

- 将管理员后台拆分为独立 `/admin` 入口，客户页不再暴露后台导航和用户管理能力。
- 增强后台配置能力，支持维护模型路由、积分规则、充值套餐、用户额度、存储/WebDAV 策略和系统提示词模板。
- 优化客户工作台体验：参考图支持拖拽，生成结果改为右侧主图加缩略图，任务系统改为顶部弹层并展示结果缩略图。
- 调整提示词优化流程，前台仅展示可编辑用户提示词，系统提示词和参考图映射在生成时内部拼接。
- 更新 README、商用就绪说明、需求验收清单和 smoke 脚本，覆盖独立后台、前台隐藏系统提示词和任务弹层流程。

## V0.1.0 - 2026-05-13

- 发布 ClothDesign AI 商业化 MVP：单页服装 AI 生图工作台，包含模式选择、提示词、A/B/C 参考图、参数配置、任务轨和结果画廊。
- 增加 Node 服务端 `/api/generate` 代理，默认端口 `8888`，API Key 仅由服务端读取；无 Key 时自动进入演示模式。
- 增加后台模型路由、账户充值与积分、存储生命周期和 WebDAV 策略等商业化管理界面。
- 增加需求完成清单、商用就绪说明、TypeScript/Vite 构建配置和 smoke 自动化验证脚本。
